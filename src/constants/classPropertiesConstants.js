// Alignment Properties
const CENTER_ALIGNMENT = "align_center";
const LEFT_ALIGNMENT = "align_left";
const RIGHT_ALIGNMENT = "align_right";

// Color Properties
const BLUE_COLOR = "color_blue";
const RED_COLOR = "color_red";

// Size Properties
const SM_SIZE = "size_sm";
const MD_SIZE = "size_md";
const LG_SIZE = "size_lg";

export const alignmentProperties = {
  CENTER_ALIGNMENT,
  LEFT_ALIGNMENT,
  RIGHT_ALIGNMENT,
}

export const colorProperties = {
  BLUE_COLOR,
  RED_COLOR
}

export const sizeProperties = {
  SM_SIZE,
  MD_SIZE,
  LG_SIZE,
}